import React, { Component } from "react";
import { View, Text } from "native-base";

class DeviceList extends Component {
  render() {
    return (
      <View>
        <Text>Device list - logged in</Text>
      </View>
    );
  }
}

export default DeviceList;
